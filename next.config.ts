import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  turbopack: {
    root: "C:/Users/nguye/OneDrive/Máy tính/test2/my-apply", // Đường dẫn tuyệt đối đến thư mục dự án
  },
};

export default nextConfig;
